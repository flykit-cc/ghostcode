// Shared visual tokens — one place to tune the look of every screen.

// Brand accent — wordmark, selection arrows, input cursors.
export const ACCENT = "magenta";

// Neutral row-highlight for the focused row. Reads as "selected" without
// fighting per-project tint colors the way a saturated bg does.
export const SELECTION_BG = "#3a4150";
